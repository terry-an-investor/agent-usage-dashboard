# Commit 审计报告 — 最近 6 个提交（含两轮修复）

- **日期**：2026-09-15 ~ 09-16
- **审计对象**：`ac2e9d3`（测试套件）、`7150264`（数据正确性 + 性能）、`6dd2dad`、`671b8c5`
  （模型筛选口径），以及针对审计结论产生的两轮修复 `99d5787`、`67f52f5` 与 `0082b3e`、`6fb20ba`
- **审计方式**：三条独立 subagent 线（采集端 / 前端 / 测试质量）+ 主线的独立验证
  （跑测试、对真实 `data.js` 复算、真实浏览器 live test）；第二轮再派三条线**审计我的修复本身**
- **真实数据规模**（审计时）：13 agents / 124 日行 / 1249 会话 / 147 项目行 / 48 个项目标签

## 声明

本轮**不是只读审计**：审计结论经过两轮修复并已提交（4 个 commit）。`data.js` 是生成物
（`.gitignore`），审计期间被 `./refresh.sh` 重写过；仓库里没有任何个人数据被提交。

审计过程中有一次副作用需要记录：一条验证线在实验时误把 `.cache/collect-state.pkl`
覆盖成废载荷（源码与 `data.js` 未动）。已跑一次 `./refresh.sh` 恢复，并据此给测试加了
`setUpModule()` 兜底重定向 —— 测试现在不可能再写到用户真实的 `.cache`。

---

## 一、结论摘要

| # | 级别 | 问题 | 模块 | 状态 |
| --- | --- | --- | --- | --- |
| H1 | **高** | 增量缓存指纹 fail-closed 过宽：库不存在 / 0 字节空库 / 表不存在都被判死 → 整机缓存**永久禁用**，每次全量重算且界面无感 | 缓存 | 已修 |
| H2 | **高** | 「采集后重算指纹」会把采集窗口内的并发写入吸收进指纹 → 下次命中缓存时那批新数据**永久缺失** | 缓存 | 已修（撤销） |
| M1 | 中 | commandcode 的 `tot` 累加写在日期守卫**之前** → 非法日期时"会话之和 > 日之和" | 采集 | 已修 |
| M2 | 中 | antigravity 对毫秒时间戳直接 `fromtimestamp` → `ValueError` 冒泡，整个来源剩余库全不解析 | 采集 | 已修 |
| M3 | 中 | cursor TTL 只在采集内检查，而采集只在缓存未命中时发生 → 数据可能长期停滞；`TTL=0` 被架空 | 缓存 | 已修 |
| M4 | 中 | 指纹在采集前算、状态在采集后存，而采集会重写 cursor CSV → 每次真实拉取后白付一次全量 | 缓存 | 已修 |
| M5 | 中 | 项目名跨来源三套口径 → 同一真实项目在项目下拉里出现 3 个标签，下钻占比被稀释 | 口径 | UI 说明（见 §5） |
| M6 | 中 | 判"该来源是否已有日粒度"用的是**按项目名**的全局 Set → 同名项目下"只有会话"的 agent 被整段挡住 | 口径 | 已修 |
| M7 | 中 | 会话兜底行的合成 `modelBreakdowns` 把成本写死 `null` → 「项目+模型」双筛选下成本整段丢失 | 口径 | 已修 |
| M8 | 中 | 模型下拉计数不含会话兜底 → 下钻到仅有会话的项目时计数全 0 并置灰，而模型表有真实用量 | 口径 | 已修 |
| L3 | 低 | `aggProjects` 用 `.every(modelMatch)` 纳入多模型会话，`rebuildView` 只认单模型 → 项目表比 KPI 多出这一块 | 口径 | 已修 |
| L1/L2 | 低 | 兜底近似行的日期只有"最后活动日"，参与 `firstDate` 会把 p=all 的范围整段拉早 | 口径 | 已修 |
| — | 低 | 同一环境变量 `USAGE_DASH_CURSOR_TTL` 两处解析语义不同；`hour_of_ts` 无防御；kimix 裸 `fromtimestamp`；antigravity 忽略 `add_usage` 返回值；`warnings.append` 未持锁；`mark()` 死字段；`—` 与「未定价」两种空态混用 | 采集/前端 | 已修 |

**测试基线**：`ac2e9d3` 声称的 36 单测 / 14 e2e 与实际一致。经两轮修复后为 **55 单测 + 18 e2e**，全绿。

---

## 二、方法与证据（可复现）

| 验证手段 | 命令 / 做法 | 结论可信度 |
| --- | --- | --- |
| 单测 | `npm run test:py` | 直接可信（0.1s） |
| e2e | `npm run test:e2e`（真实 Chrome，~35s） | 直接可信 |
| 破坏性验证 | 把修复改回错误版本，看对应用例是否变红 | **7/7 变红**（见 §4） |
| 端到端缓存行为 | `./refresh.sh` + `USAGE_DASH_STATE_TTL` / `USAGE_DASH_CURSOR_TTL` 调参 | 见 §4 |
| 真实数据复算 | 对 `data.js` 独立复算不变量（会话和 vs 日和、成本并存、模型名质量） | 见 §4 |
| live test | kimi-webbridge + `http://127.0.0.1:8931`，真实数据 + 真实浏览器 | 见 §4 |

### 哪些验证是"空转"的（诚实记录）

第一轮我给出的两组 live test 证据**不具鉴别力**，由验证复核线指出后已作废：

- 「52 个项目逐个下钻，KPI 与项目表行 0 处不一致」——真实数据里满足触发条件的样本是
  **0 个**（"某 (agent, project) 有会话但无日粒度、且项目名在别的 agent 处有日粒度"）
  → 修复前后渲染完全相同，这条在旧代码上同样会通过。
- 「修复前后产物对比 124/33/48 无变化」——量纲错配（结构计数不变 ≠ 行内数值不变），
  且基准被审计期间的新增用量污染、旧产物本身可能就生成于 bug 影响下。
- 「会话和 == 日和」成立，但对 M1 无证明力（真实数据里非法日期行 = 0）。

第二轮改用**有鉴别力**的验证：破坏性验证 + 真实数据下能观察到的状态变化
（模型下拉计数 0 → 3,766/1,419）。

---

## 三、值得记录的三个判断

1. **指纹与新鲜度是两件事，混在一起会自我循环。** 指纹该回答"输入内容变没变"；
   "数据够不够新"是另一个问题。第一轮把 cursor CSV 的过期状态塞进指纹，导致
   "过期 → 指纹变 → 重算 → 拉取重写 CSV → 指纹又变"，以及"过期 + 拉取失败 → 永远命中"。
   最终方案：指纹剔除 cursor CSV，新鲜度交给状态缓存年龄（`USAGE_DASH_STATE_TTL`，默认 300s；
   `USAGE_DASH_CURSOR_TTL=0` 时不命中）。
2. **fail-closed 要卡在"可能读到新鲜数据"的那一侧。** 判错的代价不对称：
   fail-open 会静默显示旧数字；fail-closed 过宽则让缓存永久失效（性能回退但数据正确）。
   真正该 fail-closed 的只有一种情形 —— 直读失败**且** `_copy_db` 复制也失败。
   库不存在 / 空库 / 表不存在都是**可复现状态**，必须给稳定签名。
3. **跨视图一致性需要"三处同源"，不是"三处都改对"。** 同一个判断（该来源是否有日粒度）
   出现在 `rebuildView` / `aggProjects` / 模型计数三处，第一轮只改了两处；
   现在把 `dailyPairs` 提到顶层共享，代码层面无法再各写一份。

---

## 四、修复记录（4 个 commit）

| commit | 主题 | 关键改动 |
| --- | --- | --- |
| `99d5787` | 指纹 fail-closed 与采集端三处静默错误 | 第一轮修复（其中 H1/H2 的修法后被审计推翻） |
| `67f52f5` | 项目下钻按 (agent, project) 判重 + 项目名说明文案 | M6 修复 |
| `0082b3e` | 缓存新鲜度与指纹职责分离，采集端补齐同类隐患 | H1/H2/M3/M4 重做 + 同类项 |
| `6fb20ba` | 项目下钻口径收口，补齐成本/计数/日期范围连带问题 | M7/M8/L1/L2/L3 + 空态文案 |

### 破坏性验证（改回错误版本 → 用例变红）

| 改坏的地方 | 被抓住的用例 | 失败信息 |
| --- | --- | --- |
| 合成 mb 成本写死 `null` | M3c | 成本应为 0.3，实际 0 |
| `mTot` 去掉会话兜底 | M3c | 模型计数不应全为 0：`["0"×6]` |
| `aggProjects` 改回按项目名判重 | M3b | 项目表该行总量(1000)应与 KPI(1400)同口径 |
| `firstDate` 含近似行 | L2 | 范围起点应为 2026-09-15，而不是 2026-08-17 |
| 项目表用 `.every()` 纳入多模型会话 | L3 | 应与 KPI 同口径（kpi=0 row=500） |
| 指纹没有稳定签名 / 取不出时不 fail-closed / 状态缓存不看年龄 | 单测 9 条 | 见 `tests/test_collect.py` |

### 端到端行为（`./refresh.sh`）

```
冷启动（清 .cache）      3.00s   全量
第 2、3 次               0.23s / 0.21s   命中（不再"多付一次全量"）
state 弄旧 10 分钟       1.15s   全量（年龄超限）
USAGE_DASH_CURSOR_TTL=0  1.89s   全量（尊重"每次拉最新"）
```

### 真实数据 live test（有鉴别力）

- 模型下拉计数：真实仅有会话的项目 `--Users-zhaozhoubin-dim-agent--` 下从**全 0** 变为
  `3,766 / 1,419`（正是该项目的两条 pi 会话）
- 空态区分：`Devin` 行显示「未定价」（有 token 无成本），`Cursor` 行显示 `—`（该范围无 token）
- 不变量复算：会话之和 == 日之和（差值 0）；`hourly` / `daily` / `modelBreakdowns`
  均无"记账与估算并存"；模型名 33 种未变

---

## 五、未修项与已知局限

| 项 | 为什么没修 |
| --- | --- |
| 指纹对"只 UPDATE、不 INSERT"是盲的 | 要覆盖得给大表加 `SUM(LENGTH(...))`，代价与收益不成比例；采集读取的列都在 INSERT 时写入，注释已写明局限 |
| `_load_state` 用 `pickle` 反序列化本地 `.cache/collect-state.pkl` | 能写该文件即可执行代码；本机单用户 + 文件权限 600，属低危 |
| 项目名跨来源分裂（`Desktop/trading-logic` / `~/desktop-trading-logic` / `trading-logic`） | 采集侧**无法无损还原**（cursor transcript 只有 `message`/`role` 没有路径字段；目录内 `.workspace-trusted` 仅 2/9 有 `workspacePath`；commandcode 目录名已全小写，大小写与路径段边界丢失）。**后续（`9814220`）改为在展示层归并**：按小写段序列的后缀关系合并，两边都需 ≥2 段以避开 1 段短名吞并（即 `7150264` 修过的撞标签），代表名优先"像路径"的写法；实测 `trading-logic` 三种写法并成一行（agents 四种）、项目下拉 53 → 35 项，4 个 worktree 变体未被误吞 |
| 兜底近似行的总量不按天数摊平 | 会话只记了最后活动日，摊平就是假精度。日柱图在该日偏高的语义已写进注释与 `AGENTS.md` |
| `?p=all&pj=X` 直开 URL 与"切项目后点全部"的 `rangeStart` 来源不同 | 实测发现的既有行为（前者用"全部视图"的 firstDate，因为 `applyPreset` 跑在 `rebuildView` 之前）；影响仅限于范围起点，未动 |
| 运行中的 8931 是旧 Python 实例 | 行为实测与 HEAD 一致；建议 `./start.sh` 重启以消除"跑的不是当前代码"的混淆 |

---

## 六、过程复盘（委派）

- 第一轮把"审 3 个 commit × 2375 行 + 逐条验证"整包给单个 agent，并在提示里开了浏览器
  许可 → 该线自建 CDP 驱动 + fixture server 反复跑，十几分钟无产出。教训：**审计任务要给
  硬性验证预算**（禁止清单 + 次数上限 + 时间预算），软约束（"优先""避免"）拦不住"更彻底"的路径。
- 第二轮拆成窄任务后，三条线均在预算内给出可用结论，且**相互独立地收敛到同一结论**
  （fail-closed 过宽、cursor 拉取失败后不再重试），可信度显著高于单线。
- 让 subagent 审自己的修复是有效的：它抓出了两处修法错误（H1/H2）、两处修法不完整
  （M3 只覆盖一半）、一处"修得对但守不住"（`aggProjects` 无断言）。

---

## 附：复现本轮验证

```bash
npm test                        # 55 单测 + 18 e2e
./refresh.sh                    # 重新生成 data.js（改过 collect.py 必须跑）
time ./refresh.sh               # 应命中缓存（~0.2s）
USAGE_DASH_STATE_TTL=0 ./refresh.sh   # 应全量重算
USAGE_DASH_CURSOR_TTL=0 ./refresh.sh  # 应全量重算
```
